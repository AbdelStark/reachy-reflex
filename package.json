{
  "name": "reachy-reflex",
  "version": "0.0.1",
  "description": "Realtime social attention and turn-taking for Reachy Mini",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": { "build": "tsc -p tsconfig.json", "check": "tsc -p tsconfig.json --noEmit", "test": "npm run build && node --test test/*.test.mjs" },
  "dependencies": { "reachy-jev": "file:../reachy-jev" },
  "devDependencies": { "typescript": "^5.9.0" },
  "repository": { "type": "git", "url": "https://github.com/AbdelStark/reachy-reflex.git" }
}
